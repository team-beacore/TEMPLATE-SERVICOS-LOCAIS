/**
 * Componente de ícone reutilizável.
 *
 * A implementação (registro de SVGs e tipo `IconName`) vive em `../lib/icons`
 * e é reexportada aqui como porta de entrada pública para os componentes,
 * mantendo uma única fonte de verdade.
 */
export { Icon } from '../lib/icons'
export type { IconName } from '../lib/icons'